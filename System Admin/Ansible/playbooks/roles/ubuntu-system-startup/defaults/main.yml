---

shell: /bin/bash

packages:
  - apt-transport-https
  - curl
  - gnupg
  - vim
  - htop
  - ca-certificates
  - python3-pip

dock_packages:
  - docker-ce
  - docker-ce-cli
  - containerd.io
  - docker-buildx-plugin
  - docker-compose-plugin
